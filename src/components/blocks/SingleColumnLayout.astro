---
import ColumnContent from './ColumnContent.astro';
import { getColumnLayoutStyle, type ColumnLayoutItem, type ColumnLayoutStyleProps } from './columnLayoutTypes';
import { getSpacerClasses } from './layoutOptions';

interface Props extends ColumnLayoutStyleProps {
  column1?: ColumnLayoutItem | null;
}

const { column1 = null, textColor, backgroundColor, topSpacer, bottomSpacer } = Astro.props;
const wrapperStyle = getColumnLayoutStyle({ textColor, backgroundColor });
const spacerClasses = getSpacerClasses(topSpacer, bottomSpacer);
---

<section class="Section">
  <div class:list={['Grid', spacerClasses]} style={wrapperStyle}>
    <div class="col-span-6 flex flex-col gap-8 tablet:col-span-8 desktop:col-start-3">
      <ColumnContent column={column1} />
    </div>
  </div>
</section>
