---
interface Props {
  src: string;
  alt: string;
  element?: 'div' | 'span';
  srcset?: string;
  sizes?: string;
  width?: number;
  height?: number;
  loading?: 'eager' | 'lazy';
  fetchpriority?: 'high' | 'low' | 'auto';
  decoding?: 'sync' | 'async' | 'auto';
  class?: string;
  preloadClasses?: string;
  imgClass?: string;
  fadeIn?: boolean;
}

const { src, alt, element = 'div', srcset, sizes, width, height, loading = 'lazy', fetchpriority, decoding = loading === 'eager' ? 'sync' : 'async', class: className, preloadClasses = '', imgClass, fadeIn = false, ...rest } = Astro.props;
const Wrapper = element;
---

<Wrapper class:list={[preloadClasses, className]}>
  <img src={src} srcset={srcset} sizes={sizes} alt={alt} width={width} height={height} loading={loading} fetchpriority={fetchpriority} decoding={decoding} class:list={[imgClass, { 'fade-in-500': fadeIn }]} {...rest} />
</Wrapper>
