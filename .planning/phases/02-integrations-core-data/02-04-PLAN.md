---
phase: 02-integrations-core-data
plan: 4
title: 'Room Components: AmenityBadge, RoomCard, RoomGallery'
type: execute
wave: 2
depends_on:
  - 02-02
autonomous: true
requirements:
  - ROOM-02
  - ROOM-03

must_haves:
  truths:
    - 'AmenityBadge renders a text-only badge with correct Tailwind classes from UI-SPEC'
    - "RoomCard renders thumbnail at 600x400, room name, rate formatted as 'From $X / night', and up to 3 AmenityBadge atoms"
    - 'RoomGallery renders first photo eager at 1200x800, remaining photos (up to 5 more) lazy in a responsive grid'
    - 'Unknown amenity IDs are never rendered — caller passes only resolved strings'
    - 'All Image components have non-empty alt text'
  artifacts:
    - path: 'src/components/AmenityBadge.astro'
      provides: 'Single amenity badge atom'
    - path: 'src/components/RoomCard.astro'
      provides: 'Room listing card with photo, name, rate, badges, CTA'
    - path: 'src/components/RoomGallery.astro'
      provides: 'Room detail photo gallery — static scrollable grid'
  key_links:
    - from: 'src/components/RoomCard.astro'
      to: 'src/components/AmenityBadge.astro'
      via: 'import and render up to 3 badges'
      pattern: 'AmenityBadge'
    - from: 'src/components/RoomCard.astro'
      to: 'src/lib/hostaway.ts'
      via: 'Props typed against HostawayRoom fields (id, name, price, photos, amenityNames)'
      pattern: 'HostawayRoom'
---

<objective>
Create the three room-related Astro components. These are purely presentational — they receive typed props and render HTML. No data fetching inside components.

Purpose: Room pages (Wave 3) compose these atoms/molecules. Building components first means Wave 3 plans can reference concrete component APIs without ambiguity.
Output: AmenityBadge.astro, RoomCard.astro, RoomGallery.astro
</objective>

<execution_context>
@/Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-UI-SPEC.md
@/Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-CONTEXT.md
</execution_context>

<context>
@/Users/akw/Documents/09-dev/bristol-inn/.planning/ROADMAP.md
@/Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-RESEARCH.md

<interfaces>
<!-- HostawayRoom (from src/lib/hostaway.ts — read this file before coding): -->
export interface HostawayRoom {
  id: number;
  name: string;
  description: string;
  price: number;
  photos: Array<{ url: string; caption: string; sortOrder: number }>;
  amenityNames: string[];
  bedroomsNumber: number;
  bathroomsNumber: number;
  personCapacity: number;
}

<!-- AmenityBadge props (from UI-SPEC): -->

interface Props { label: string; }

<!-- AmenityBadge classes: inline-flex items-center px-2 py-0.5 rounded text-xs font-bold bg-gray-100 text-slate-700 -->

<!-- RoomCard props (from UI-SPEC): -->

interface Props {
id: number;
name: string;
rate: number; // = room.price
photo: { url: string; caption: string }; // = room.photos[0]
amenities: string[]; // = room.amenityNames.slice(0, 3) — caller slices, not card
}

<!-- RoomCard classes:
  Card container: bg-white rounded-lg overflow-hidden shadow-sm border border-gray-200 hover:shadow-md
  Content area: p-4 flex flex-col gap-3
  Room name: h3 class="text-xl font-bold text-gray-900"
  Rate: text-base font-normal text-gray-700
  Badge row: flex flex-wrap gap-2
  CTA link: inline-block mt-2 text-slate-700 font-normal underline-offset-2 hover:underline
  CTA href: /rooms/{id}
  CTA copy: "View Room"
-->

<!-- RoomGallery props (from UI-SPEC): -->

interface Props {
photos: Array<{ url: string; caption: string }>; // up to 6 total
roomName: string;
}

<!-- RoomGallery classes:
  First photo: w-full object-cover — Image width=1200 height=800 loading="eager" format="webp"
  Secondary grid: grid grid-cols-2 tablet:grid-cols-3 gap-2 mt-2
  Photos 2–6: loading="lazy" format="webp" width=1200 height=800
  Alt: photo.caption || roomName + " — photo " + (index + 1)
-->

<!-- Tailwind breakpoints: tablet: 640px, desktop: 1024px — NOT md: or lg: -->
<!-- Image component import: import { Image } from 'astro:assets'; -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AmenityBadge.astro and RoomCard.astro</name>
  <files>src/components/AmenityBadge.astro, src/components/RoomCard.astro</files>
  <read_first>
    - /Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-UI-SPEC.md (AmenityBadge and RoomCard component specs — exact classes, props, copy)
    - /Users/akw/Documents/09-dev/bristol-inn/src/lib/hostaway.ts (HostawayRoom interface — RoomCard props derive from this type)
    - /Users/akw/Documents/09-dev/bristol-inn/src/components/Header.astro (existing component pattern — see how frontmatter Props interface is declared)
    - /Users/akw/Documents/09-dev/bristol-inn/tailwind.config.mjs (confirm tablet: and desktop: breakpoint names)
  </read_first>
  <action>
    Create src/components/AmenityBadge.astro:
    - Props interface: `interface Props { label: string; }`
    - Template: `<span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-bold bg-gray-100 text-slate-700">{label}</span>`
    - No icon, no JS.

    Create src/components/RoomCard.astro:
    - Props interface (as shown in interfaces block — id: number, name: string, rate: number, photo: {url, caption}, amenities: string[])
    - Import `{ Image }` from 'astro:assets'
    - Import AmenityBadge from './AmenityBadge.astro'
    - Card container: `<article class="bg-white rounded-lg overflow-hidden shadow-sm border border-gray-200 hover:shadow-md">`
    - Image: `<Image src={photo.url} alt={photo.caption || name} width={600} height={400} format="webp" loading="lazy" class="w-full object-cover" />`
    - Content div: `<div class="p-4 flex flex-col gap-3">`
    - Room name: `<h3 class="text-xl font-bold text-gray-900">{name}</h3>`
    - Rate: `<p class="text-base font-normal text-gray-700">From ${rate} / night</p>` — note: rate is a number (dollars), format as integer with no decimals using `Math.round(rate)` or `rate.toFixed(0)` to avoid "From 150.00 / night"
    - Badge row: `<div class="flex flex-wrap gap-2">` with `{amenities.map(a => <AmenityBadge label={a} />)}`
    - CTA: `<a href={`/rooms/${id}`} class="inline-block mt-2 text-slate-700 font-normal underline-offset-2 hover:underline">View Room</a>`
    - Caller is responsible for slicing amenities to 3 before passing — RoomCard renders all amenities it receives (document this in a comment)

  </action>
  <verify>
    <automated>cd /Users/akw/Documents/09-dev/bristol-inn && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AmenityBadge.astro exists with `inline-flex items-center px-2 py-0.5 rounded text-xs font-bold bg-gray-100 text-slate-700` classes
    - src/components/RoomCard.astro exists
    - RoomCard imports Image from 'astro:assets' and AmenityBadge from './AmenityBadge.astro'
    - RoomCard uses `<article>` element (semantic HTML)
    - RoomCard renders `<h3>` for room name (correct heading level inside listing page's h1)
    - RoomCard Image has width=600 height=400
    - RoomCard CTA text is "View Room" and href is /rooms/{id}
    - Rate formatted without trailing decimals (e.g. "From $150 / night" not "From $150.00 / night")
    - npm run build exits 0
  </acceptance_criteria>
  <done>AmenityBadge and RoomCard components created with correct classes, props, and semantic HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Create RoomGallery.astro</name>
  <files>src/components/RoomGallery.astro</files>
  <read_first>
    - /Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-UI-SPEC.md (RoomGallery component spec — primary photo, secondary grid, alt fallback format)
    - /Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-CONTEXT.md (D-04: up to 6 photos; D-15: 1200x800 gallery photos)
    - /Users/akw/Documents/09-dev/bristol-inn/src/components/RoomCard.astro (just created — reference for Image import pattern)
    - /Users/akw/Documents/09-dev/bristol-inn/tailwind.config.mjs (confirm tablet: breakpoint name for secondary grid)
  </read_first>
  <action>
    Create src/components/RoomGallery.astro.
    Props interface: `interface Props { photos: Array<{ url: string; caption: string }>; roomName: string; }`
    Import `{ Image }` from 'astro:assets'.

    Template logic:
    - photos may have 0 to 6 items (already sliced to 6 max by hostaway.ts normalization)
    - If photos.length === 0: render a `<div class="bg-gray-100 rounded h-64 flex items-center justify-center"><p class="text-gray-500">No photos available</p></div>`
    - First photo (photos[0]): `<Image src={photos[0].url} alt={photos[0].caption || roomName + " — photo 1"} width={1200} height={800} format="webp" loading="eager" class="w-full object-cover rounded" />`
    - Secondary photos (photos.slice(1)): wrapped in `<div class="grid grid-cols-2 tablet:grid-cols-3 gap-2 mt-2">`, each: `<Image src={p.url} alt={p.caption || roomName + " — photo " + (i + 2)} width={1200} height={800} format="webp" loading="lazy" class="w-full object-cover rounded" />`
    - No JavaScript. No carousel. Static scrollable grid only (carousel is Phase 5 per ROOM-06).
    - Use Astro's `{photos.slice(1).map((p, i) => ...)}` pattern for the secondary grid.

  </action>
  <verify>
    <automated>cd /Users/akw/Documents/09-dev/bristol-inn && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/RoomGallery.astro exists
    - Component imports Image from 'astro:assets'
    - First photo uses loading="eager"
    - Secondary photos use loading="lazy"
    - All Image elements have width=1200 height=800
    - Alt text uses `photo.caption` with fallback to `roomName + " — photo " + n`
    - Secondary grid uses class "grid grid-cols-2 tablet:grid-cols-3 gap-2 mt-2" (tablet: not md:)
    - Empty photos case renders a fallback div
    - No JavaScript in this file
    - npm run build exits 0
  </acceptance_criteria>
  <done>RoomGallery renders up to 6 photos: first eager, rest lazy, in a responsive static grid. No JS.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                       |
| ------------------------------------ | ------------------------------------------------------------------------------------------------- |
| Hostaway photo URL → Image component | URLs come from Hostaway API; domain must be in image.remotePatterns or Astro refuses to process   |
| Component props → HTML output        | Props are typed; no user input reaches components in Phase 2 (all data from build-time API calls) |

## STRIDE Threat Register

| Threat ID | Category               | Component                       | Disposition | Mitigation Plan                                                                                                                     |
| --------- | ---------------------- | ------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-02-11   | Information Disclosure | Image alt text                  | accept      | Alt text is from API data; no secrets in alt attributes; worst case is an empty fallback string                                     |
| T-02-12   | Tampering              | Hostaway photo URLs in src prop | mitigate    | astro.config.mjs image.remotePatterns allowlist prevents Astro from proxying images from unauthorized domains (added in plan 02-01) |

</threat_model>

<verification>
After both tasks complete:
- `grep "AmenityBadge" /Users/akw/Documents/09-dev/bristol-inn/src/components/RoomCard.astro` returns import and usage lines
- `grep "loading=\"eager\"" /Users/akw/Documents/09-dev/bristol-inn/src/components/RoomGallery.astro` returns 1 match (first photo only)
- `grep "tablet:grid-cols" /Users/akw/Documents/09-dev/bristol-inn/src/components/RoomGallery.astro` returns 1 match
- `npm run build` exits 0
</verification>

<success_criteria>

- AmenityBadge.astro: text-only badge with exact UI-SPEC classes
- RoomCard.astro: thumbnail 600x400, h3 name, rate "From $X / night", up to N badges, "View Room" CTA
- RoomGallery.astro: first photo eager 1200x800, secondary lazy grid, empty state fallback, no JS
- npm run build exits 0
  </success_criteria>

<output>
After completion, create `/Users/akw/Documents/09-dev/bristol-inn/.planning/phases/02-integrations-core-data/02-04-SUMMARY.md`
</output>
